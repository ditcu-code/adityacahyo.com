export enum Platform {
    ios = "iOS app",
    iosAndroid = "iOS & Android app",
    ipados = "iPadOS app",
    watchos = "watchOS app",
    web = "web",
    both = "web & app"
}

export enum EdgeConfig {
    greeting = "greeting"
}